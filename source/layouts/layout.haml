!!! 5
/[if lt IE 7] <html class="no-js lt-ie9 lt-ie8 lt-ie7 lang="#{data.site.lang}"">
/[if IE 7]    <html class="no-js lt-ie9 lt-ie8 lang="#{data.site.lang}"">
/[if IE 8]    <html class="no-js lt-ie9 lang="#{data.site.lang}"">
<!--[if gt IE 8]><!-->
%html{:class => "no-js", :lang => "#{data.site.lang}"}
  <!--<![endif]-->
  %head
    %meta{:charset => "utf-8"}
    %meta{'http-equiv' => "X-UA-Compatible", :content =>"IE=edge,chrome=1"}

    -# Pull in the page title
    %title
      = data.site.title unless data.page.title?
      = data.page.title + ' - ' + data.site.title if data.page.title?

    %meta{:name => "description", :content => "#{data.site.description}"}
    %meta{:name => "author", :content => "#{data.site.author}"}
    %meta{:name =>"viewport", :content => "width=device-width"}

    = stylesheet_link_tag "main"
    = javascript_include_tag "application-top"

  %body
    .container.content{:role => "main"}

      -# Render Content here.
      %h1= data.page.test
      = yield

      %footer

    = javascript_include_tag "application-bottom"
